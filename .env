# MSFS Copilot Assistant Configuration

# Server Configuration
# Use 0.0.0.0 to allow network access from other devices (Mac, tablets, etc.)
# Use 127.0.0.1 or localhost for local-only access
HOST=0.0.0.0
PORT=3000

# SimConnect Configuration
# SimConnect will automatically detect MSFS2024 if running on the same machine
# No additional configuration needed for local connections

# Network Configuration
# Set to true to display local IP address on startup
SHOW_NETWORK_INFO=true

# Application Settings
NODE_ENV=development